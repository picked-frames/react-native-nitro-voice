{
  "name": "react-native-nitro-voice-root",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "specs": "npm run specs --prefix packages/react-native-nitro-voice",
    "example": "npm run --prefix example"
  }
}
