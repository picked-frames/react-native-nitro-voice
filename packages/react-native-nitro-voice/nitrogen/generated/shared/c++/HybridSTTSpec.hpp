///
/// HybridSTTSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `STTConfig` to properly resolve imports.
namespace margelo::nitro::nitrovoice { struct STTConfig; }

#include <NitroModules/Promise.hpp>
#include "STTConfig.hpp"
#include <string>
#include <functional>
#include <NitroModules/ArrayBuffer.hpp>

namespace margelo::nitro::nitrovoice {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `STT`
   * Inherit this class to create instances of `HybridSTTSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSTT: public HybridSTTSpec {
   * public:
   *   HybridSTT(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSTTSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSTTSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSTTSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> initialize(const STTConfig& config) = 0;
      virtual std::shared_ptr<Promise<void>> startStreaming(const std::function<void(const std::string& /* text */)>& onPartial, const std::function<void(const std::string& /* text */)>& onFinal) = 0;
      virtual std::shared_ptr<Promise<void>> startVADGated(const std::string& vadModelPath, const std::function<void(const std::string& /* text */)>& onTranscript) = 0;
      virtual void feedAudio(const std::shared_ptr<ArrayBuffer>& samples, double sampleRate) = 0;
      virtual std::shared_ptr<Promise<void>> startMic() = 0;
      virtual void stopMic() = 0;
      virtual std::shared_ptr<Promise<void>> stop() = 0;
      virtual std::shared_ptr<Promise<void>> destroy() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "STT";
  };

} // namespace margelo::nitro::nitrovoice
