///
/// HybridTTSSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `TTSConfig` to properly resolve imports.
namespace margelo::nitro::nitrovoice { struct TTSConfig; }

#include <NitroModules/Promise.hpp>
#include "TTSConfig.hpp"
#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include <functional>

namespace margelo::nitro::nitrovoice {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `TTS`
   * Inherit this class to create instances of `HybridTTSSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridTTS: public HybridTTSSpec {
   * public:
   *   HybridTTS(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridTTSSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridTTSSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridTTSSpec() override = default;

    public:
      // Properties
      virtual double getSampleRate() = 0;
      virtual double getNumSpeakers() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> initialize(const TTSConfig& config) = 0;
      virtual std::shared_ptr<Promise<void>> speak(const std::string& text, const std::function<void(const std::shared_ptr<ArrayBuffer>& /* samples */, double /* sampleRate */)>& onAudioChunk, const std::function<void()>& onComplete) = 0;
      virtual std::shared_ptr<Promise<void>> stop() = 0;
      virtual std::shared_ptr<Promise<void>> destroy() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "TTS";
  };

} // namespace margelo::nitro::nitrovoice
